Fix critical bugs on vestcalc.com homepage (index.html):

PROBLEM: When filtering by category, ad slots multiply and push tool cards off screen. Only 1 tool card shows but 9+ ad slots appear.

FIX THESE ISSUES:

1. AD SLOTS - Complete overhaul:
   - Remove ALL dynamic/JS-inserted ad slots
   - Keep ONLY 2 static ad slots: one above the tool grid, one below the pagination bar
   - Ad slots must be hardcoded in HTML, never generated by JavaScript
   - Ad slots must NOT be inside the tool card rendering loop

2. CATEGORY FILTER BUG:
   - Check the JS filtering logic - ads must not be counted as tool cards
   - When a category is selected, only hide/show tool cards, never touch ad slots
   - Make sure the tool card count and pagination recalculates based only on actual tool cards

3. PAGINATION:
   - Show page numbers 1 2 3 4 5 between Prev/Next buttons
   - Style with green theme #0d9488, rounded buttons, centered
   - Current page highlighted in solid green

After fix, push to GitHub:
cd C:\Users\Administrator\vestcalc && git add -A && git commit -m "fix: critical ad/filter bug, redesign pagination" && git -c http.proxy=http://127.0.0.1:7897 -c http.sslVerify=false push origin master